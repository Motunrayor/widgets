import styled from "styled-components";

const Title = styled.h1`
  color: #c5cc4;
`;

const Para = styled.p`
   font-size: 15px;
   margin-top: 1rem;
`;

const Container = styled.div`
   background: #f7f7f9;
   margin: 1rem;
   border: 1px solid #000;
   padding: 0.3rem
`;

export default Title;
export {Para, Container};

